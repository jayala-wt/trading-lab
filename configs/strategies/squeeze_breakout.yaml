id: squeeze_breakout
name: Squeeze Breakout
required_patterns:
  - volatility_squeeze
  - dim_volatility_squeeze
description: Enter on volatility squeeze release. Uses both YAML and dimension-based detection.
entry:
  action: open
  side: buy
  size_mode: fixed_usd
  size_value: 20
  order_type: market
risk:
  stop_loss_pct: 0.8
  take_profit_pct: 1.8
